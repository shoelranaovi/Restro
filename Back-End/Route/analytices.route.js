const express = require('express');
const router = express.Router();


// Dashboard overview routes
// router.get('/dashboard', protect, getDashboardStats);
// router.get('/live-visitors', protect, getLiveVisitors);
// router.get('/page-views', protect, getPageViews);
// router.get('/sessions', protect, getSessionData);
// router.get('/speed', protect, getSpeedMetrics);
// router.get('/visitor-history', protect, getVisitorHistory);

// // Data recording routes
// router.post('/visitor', updateVisitorCount);
// router.post('/page-view', recordPageView);
// router.post('/session', recordSession);

module.exports = router;